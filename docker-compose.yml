services:
  node:
    build: .
    working_dir: /workspace
    env_file: .env
    volumes:
      - .:/workspace
      - pi-config:/root/.pi/agent
    stdin_open: true
    tty: true

volumes:
  pi-config:
