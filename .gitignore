# Secrets
.env

# Upstream source clone (fetched on demand — see README / `make doctor`)
FreeToken/

# Local model checkpoints served via the ./models bind mount
models/*
!models/.gitkeep

# Agent workspace contents (keep the directory itself via placeholder)
workspace/*
!workspace/.gitkeep

# Editor / OS noise
.idea/
.vscode/
*.swp
.DS_Store